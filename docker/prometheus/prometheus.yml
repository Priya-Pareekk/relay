global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'relay-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['relay-app:8080', 'host.docker.internal:8081']
        labels:
          application: 'relay'
